# S3 唯一写入者与信任域迁移测试报告

> 日期：2026-07-23
> 状态：全部代码与外部资源门禁通过；以 S3 intentional commit/push 收口

## 已验证

- 87 项 unit tests 全部通过；覆盖 Session/Control/Model/Hands/Policy/Credential/Artifact、
  workload capability、owner-only secret 注入、Admin Client 与 Runtime 无 platform egress。
- PostgreSQL S3 集成通过：幂等 migration、Hands Invocation 恢复、Policy Decision evidence、
  Credential Reference revocation、Artifact Metadata、Tool Registry、持久 Admin Operation 与
  `PUBLIC` schema create revoke。
- 已增加生产 role DSN 权限矩阵测试：校验角色不是超级用户、owner schema 实际 DML、跨 schema
  SQL 拒绝，以及 `task_query_ro` 对 Projection 只读；9 个生产角色的真实矩阵已全部通过。
- `roles.sql` 已设置未来 migration 的 default privileges，并撤销 `public` schema 的公共 CREATE，
  避免滚动升级新增表绕过 owner/read-only 边界。
- `0009` expand/migrate 已验证旧 Tool Capability、Invocation Dedup、Approval、Credential
  Reference/Usage 迁入新 owner schema；Credential 的 provider/account scope 保持不丢失。
- 在 `/tmp` 隔离 PostgreSQL 中完成 `0001...0009 up -> 新旧状态校验 -> 0009 down -> 旧状态回填`
  回滚测试；旧表保留支持 N/N-1 只读回退，owner 切换采用配置写冻结/排空而非跨 owner 双写；
  现有配置数据库未执行 destructive down。
- 已增加 Vault KV v2 adapter 单元测试，以及由真实 endpoint/token 和一次性 credential reference
  驱动的 readiness/resolve 集成测试；真实测试已通过。多字段 KV 使用显式 `path#field`，无字段
  时使用 `value` 并 fail closed；测试不记录 endpoint、token 或解析出的 Secret。
- SeaweedFS 真实 S3 往返已通过：使用短期 SigV4 URL 完成随机测试对象的
  `PUT -> HEAD -> GET -> DELETE`，对象内容校验一致且清理成功。
- 106 项完整 Pytest（87 项 unit）通过，包括 PostgreSQL/Kafka/HTTP、生产 DB role、SeaweedFS、
  Vault 与隔离 PostgreSQL migration roundtrip。
- Ruff、Mypy（142 个源码文件）和 10 条 import-linter contract 全部通过。
- 未读取或输出 `.env` 中的 Secret；`.env.example` 只有空占位符。

## 结论

S3 的功能、架构、权限、迁移、回滚、外部资源与质量门禁全部通过，可以形成单一 intentional
commit 并 push 到当前分支。

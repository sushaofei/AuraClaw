# M7 前端测试与监控工作台测试报告

## 结论

M7 前端工作台的功能、前端构建、协议测试、浏览器冒烟、架构边界和安全脱敏校验通过。
Python 后端未修改；Ruff、Mypy、41 项单元回归与 Release Gate 均通过。

全量后端回归共 53 项，其中 52 项通过。既有
`test_postgres_control_claim_lease_fencing_checkpoint_and_capacity` 在远程 PostgreSQL 环境连续两次
因 5 秒测试租约在多次网络往返期间过期而失败；失败发生在保存 checkpoint 之前，与本阶段前端代码
无调用或依赖关系。本阶段未通过放宽 fencing 生产约束或改写既有测试来掩盖该环境时延问题。

## 前端自动化

- ESLint：通过。
- Vinext Cloudflare Worker 生产构建：通过。
- Node 测试：5 项通过。
  - API Base URL 与唯一命令 ID。
  - SSE 跨 chunk、多行 data 解析。
  - 敏感 Header/Payload 递归脱敏与安全 curl。
  - Timeline kind/关联字段过滤。
  - Metric 时序排序、latest 选择和服务端渲染冒烟。
- Starter loading skeleton、`codex-preview` 和 `react-loading-skeleton` 已从交付物移除。

## 浏览器冒烟

使用内存后端和生产构建产物完成：

1. 页面加载并恢复非敏感 API/tenant/actor 配置。
2. `live` 返回 `ok`，`ready` 返回 `ready`，页面显示 API online。
3. 创建任务返回 Session ID，页面随后自动读取完整 Task View。
4. Projection 在创建后显示 `v2`，无需手动刷新。
5. 请求历史记录健康检查、任务创建与 Task Query，并展示 status、耗时和 traceparent。

## 后端回归

- Ruff：通过。
- Mypy strict：57 个源码文件通过。
- Unit Pytest：41 项通过，1 个 Starlette/httpx2 弃用警告。
- Full Pytest：52 项通过、1 项远程 PostgreSQL 租约时延失败。
- Release Gate：交付物、架构边界和 Secret 扫描通过。

## 安全与架构

- 前端只使用公开 HTTP/SSE API，不导入 `auraclaw.*`。
- Runtime Event 在 UI 中明确标注为非权威信息。
- Secret 不写入 localStorage、URL、控制台或持久请求历史。
- 复制 curl 会移除敏感 Header，并递归脱敏常见敏感字段。
- 高风险写操作展示 tenant/session 并二次确认。
- `.env`、`.history`、`node_modules`、构建产物、缓存和临时预览脚本不在交付范围。
